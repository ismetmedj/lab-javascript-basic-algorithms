// Iteration 1: Names and Input
const hacker1 = "mmwick";
console.log(`The driver's name is ${hacker1}`);

const hacker2 = "mmwick";
console.log(`The navigator's name is ${hacker2}`);

// Iteration 2: Conditionals
if (hacker1.length > hacker2.length) {
  console.log(
    `the driver has the longer name, it has ${hacker1.length} charather.`
  );
}
if (hacker1.length < hacker2.length) {
  console.log(
    `it seems that the navigator has the longest name, it has   ${hacker2.length} character.`
  );
}
if (hacker1.length === hacker2.length) {
  console.log(
    `wow you both have equally long names,   ${hacker2.length} character.`
  );
}

// Iteration 3: Loops
